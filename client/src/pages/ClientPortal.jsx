/**
 * ClientPortal Page Component
 * 
 * This component serves as a wrapper around the client portal functionality,
 * facilitating multi-tenant operations for CRO and client organizations.
 */

import React from 'react';
import ClientPortalComponent from '../components/client-portal/ClientPortal';

const ClientPortal = () => {
  return <ClientPortalComponent />;
};

export default ClientPortal;