/**
 * ClientPortalDashboard Page Component
 * 
 * This component serves as the dashboard for the client portal,
 * showing client-specific metrics and activities.
 */

import React from 'react';
import ClientDashboard from '../components/client-portal/ClientDashboard';

const ClientPortalDashboard = () => {
  return <ClientDashboard />;
};

export default ClientPortalDashboard;